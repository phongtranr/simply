<div class="site-post py-vmin8 px-4" data-load-more>
    <div class="js-feed-entry mx-auto max-w-1100 grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {{#foreach posts}}
            {{> "story/story-grid"}}
        {{/foreach}}
    </div>

    <button type="button" class="js-load-more button is-primary h-12 w-56 mt-14 mx-auto hidden">{{t "Load More"}}</button>

    {{!-- Pagination - partials/pagination.hbs --}}
    {{pagination}}
</div>
