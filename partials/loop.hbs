<div class="site-post py-vmin8 px-4">
    <div class="js-feed-entry mx-auto grid w-full grid-cols-1 gap-6 max-w-1100 md:grid-cols-2 lg:grid-cols-3">
        {{#foreach posts}}
            {{> "story/story-grid"}}
        {{/foreach}}
    </div>

    {{!-- Pagination - partials/pagination.hbs --}}
    {{pagination}}
</div>